services:
  interview-lab:
    build: .
    ports:
      - '8787:8787'
    env_file:
      - .env
    volumes:
      - interview-lab-data:/app/data
    restart: unless-stopped

volumes:
  interview-lab-data:
