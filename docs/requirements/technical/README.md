# requirements/technical/

Requisitos técnicos e de arquitetura (não é documentação operacional de
deploy — isso é `docs/ops/`). Vazia até o primeiro documento chegar — ver
`docs/requirements/README.md`.
